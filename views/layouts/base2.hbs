<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>

</head>

<body>
    {{#if flash.success }}
        {{>Flash}}
    {{/if}}

    {{#if flash.error }}
        {{>Flash2}}
    {{/if}}
    
    <main >
        <div class="container">
            {{{body}}}
        </div>
    </main>

</body>

</html>